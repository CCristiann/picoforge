# Model weights — never committed; SHA256s live in docs/weights.lock
models/

# Python virtualenv — reproducible from tools/requirements.txt
tools/venv/
__pycache__/
*.pyc

# Build artifacts — reproducible from source
build/
*.o
*.a
*.dylib
*.metallib
tools/probe/metal4_probe

# macOS noise
.DS_Store

# NOTE: bench/ CSVs and plots are deliberately NOT ignored — measured
# numbers are the public artifact of this project.

# Compiled engine binary
/picoforge
/tests/test_ops
